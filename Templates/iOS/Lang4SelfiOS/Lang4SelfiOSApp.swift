import SwiftUI
import Lang4SelfCore

/// Future iOS entry point. Not included in the current build.
@main
struct Lang4SelfiOSApp: App {
    var body: some Scene {
        WindowGroup {
            NavigationStack {
                ContentView()
            }
        }
    }
}
