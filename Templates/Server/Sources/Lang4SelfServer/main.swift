import Foundation
import Lang4SelfCore

/// Future server entry point. It deliberately starts no listener and stores no user data.
@main
enum Lang4SelfServerTemplate {
    static func main() {
        print("Lang4Self server template is inactive. The macOS app is fully local.")
    }
}
