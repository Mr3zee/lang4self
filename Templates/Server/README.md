# Server target template (inactive)

This package compiles an intentionally inactive executable against `Lang4SelfCore`. It is not used by the macOS app.

A future server should begin with an explicit privacy and sync design. Suggested boundaries are versioned card events, user-owned encryption keys, idempotent writes, and no redistribution of dict.cc content.
